<mvc:View xmlns="sap.m" xmlns:mvc="sap.ui.core.mvc" controllerName="dtt.SAPUI5.controller.InvoicesList">
    <List id="_IDGenList1" headerText="{i18n>invoiceListTitle}" class="sapUiResponsiveMargin" width="auto" items="{invoices>/Invoices}">
        <headerToolbar>
            <Toolbar>
                <Title text="{i18n>invoiceListTitle}" />
                <ToolbarSpacer />
                <SearchField width="50%" search=".onFilterInvoices" />
            </Toolbar>
        </headerToolbar>
        <items>
            <ObjectListItem
                id="_IDGenObjectListItem1"
                title="{invoices>Quantity}x{invoices>ProductName}"
                number="{parts:[{path:'invoices>ExtendedPrice'},{path:'Currency>/usd'}], 
                            type:'sap.ui.model.type.Currency', formatOptions:{showMeasure:false}}"
                numberUnit="{Currency>/usd}"
                numberState="{= ${invoices>ExtendedPrice}>50 ? 'Success':'Error'}"
            >
                <firstStatus>
                    <ObjectStatus text="{path:'invoices>Status',
                        formatter:'.formatter.invoiceStatus'}" />
                </firstStatus>
            </ObjectListItem>
        </items>
    </List>
</mvc:View>
